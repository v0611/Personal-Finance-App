<h1>View and update Tags for Transactions - TransactionTags junction table</h1>
<table id="transactiontags">
    <thead>
        <tr>
            <th>Transaction ID</th>
            <th>Transaction Description</th>
            <th>Tags</th>
        </tr>
    </thead>
    <tbody>
        {{#each data}}
        <tr data-value="{{this.transactionID}}">
            <td>{{this.transactionID}}</td>
            <td>{{this.description}}</td>
            <td>{{this.tags}}</td>
        </tr>
        {{/each}}
    </tbody>
</table>

<h2>Adding Tags to an existing Transaction</h2>
<p>To add a new tags for an existing transaction, please select a transaction</p>
<form id="add-new-transactiontag">
    <label for="transactionID">Transaction:</label>
    <select name="transactionID" id="transactionID">
        <option value="">Select a Transaction</option>
        {{#each transactions}}
        <option value="{{this.id}}">{{this.description}}</option>
        {{/each}}
    </select>

    <label for="tagID">Tag:</label>
    <select name="tagID" id="tagID">
        <option value="">Select a Tag</option>
        {{#each tags}}
        <option value="{{this.id}}">{{this.tagName}}</option>
        {{/each}}
    </select>

    <button type="submit">Add</button>
</form>
<script src="/js/add_transactionTags.js"></script>